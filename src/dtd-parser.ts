import {Readable} from 'stream';
/*
export class ElementTypeDeclaration {
  contentSpec: 'EMPTY' | 'ANY';

  constructor(
    public name: string,
    public contentSpec: ElementContentSpec,
  ) {
  }

  static fromText(text: string | Readable) {
    (text as ReadableStream).tee()
  }
}

export type = ElementContentSpec
*/
